{
  "name": "shippulse",
  "version": "0.5.1",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "shippulse": "dist/cli/cli.js"
  },
  "scripts": {
    "build:ts": "tsc -p tsconfig.json && cp src/server/index.html dist/server/index.html && chmod +x dist/cli/cli.js",
    "version:inject": "node scripts/inject-version.js",
    "build": "npm run build:ts && npm run version:inject",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test tests/*.test.ts",
    "security:audit": "npm audit --audit-level=high --omit=dev",
    "ci": "npm run typecheck && npm run build:ts && npm run test && npm run security:audit",
    "start": "node dist/cli/cli.js"
  },
  "dependencies": {
    "json5": "^2.2.3",
    "yaml": "^2.4.5"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "typescript": "^5.9.3"
  }
}
